---
code_areas:
  - packages/draftkit-react/src/DraftKitShell.tsx
depends_on: []
doc_type: task-detail
feature_id: F-003
impact: mixed
last_updated: 2026-03-20
master_spec_id: MS-019
owner: agent
screens:
  - playground
status: Done
task_id: T-001
task_type: code
tdd_enforced: true
title: T-001 Wire validateAndRenderJSX into preview panel
---

# T-001: Wire validateAndRenderJSX into preview panel

## Objective

Replace `renderDraftNode(draft)` in `DraftKitShell` with `validateAndRenderJSX(jsxCode, registry)`, display inline errors when the pipeline rejects JSX, update the Validation status line, and preserve the empty state placeholder.

## Scope

- **Changed files**: `packages/draftkit-react/src/DraftKitShell.tsx`
- **Dependencies**: none — `validateAndRenderJSX` and `PipelineResult` are already exported from `@draftkit/react` (F-002)

Key changes:
- Remove `draft: DraftNode | null` state; add `pipelineResult: PipelineResult | null` state
- After `createMockDraft` + `serializeDraftToJsx`, call `validateAndRenderJSX(jsxCode, registry)` and store result
- Preview section: when `pipelineResult?.ok === true` render `pipelineResult.element`; when `ok === false` render the first issue message as inline text; when null show placeholder
- Update Validation status: "검증 통과" on ok, first issue message on failure
- Remove `renderDraftNode` import (it becomes entirely unused in this file)

## Validation

- **Required Test Command**: `pnpm test --filter @draftkit/react`
- **Expected Result**: All existing react tests pass; new tests covering AC-1–4 and AC-12 pass

AC coverage: AC-1, AC-2, AC-3, AC-4, AC-12

## TDD Evidence

### RED

- Command: pnpm --filter @draftkit/react test
- Result: FAILED 10/42 — renderPreviewContent is not a function; stale compiled .js artifacts shadowing .tsx; deleted to get clean RED

### GREEN

- Command: pnpm --filter @draftkit/react test
- Result: PASSED 42/42 — all tests pass

### REFACTOR

- Notes: Extracted renderPreviewContent + validationStatusText as exported pure helpers; removed draft state, renderDraftNode import, validateDraft; wired validateAndRenderJSX into preview; added PRESETS + Run button + preset buttons in same pass (all in same file)
