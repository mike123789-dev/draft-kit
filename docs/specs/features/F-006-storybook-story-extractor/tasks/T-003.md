---
code_areas:
  - packages/draftkit-core/src/story-extractor.test.ts
depends_on:
  - T-002
doc_type: task-detail
feature_id: F-006
impact: code
last_updated: 2026-03-20
master_spec_id: MS-017
owner: agent
screens: []
status: Done
task_id: T-003
task_type: code
tdd_enforced: true
title: T-003 Integration test against real story files
---

# T-003: Integration test against real story files

## Objective

Append an integration test suite to `packages/draftkit-core/src/story-extractor.test.ts` that calls `extractRegistryFromStories` with the actual paths to all five story files under `examples/playground-next/src/components/ui/`, and asserts that the returned registry contains entries keyed `"Badge"`, `"Button"`, `"Card"`, `"Input"`, and `"Textarea"`, each with a non-empty `allowedProps` array and the correct `importPath`. This covers AC-8.

## Scope

- **Changed files**:
  - `packages/draftkit-core/src/story-extractor.test.ts` (extend) — append integration describe-block for AC-8
- **Dependencies**: T-002 must be GREEN

## Acceptance Criteria Covered

- AC-8: `extractRegistryFromStories` called with the five real story paths returns a registry with keys `"Badge"`, `"Button"`, `"Card"`, `"Input"`, and `"Textarea"`, each having a non-empty `allowedProps` array and the correct `importPath` matching the value in that story's `parameters.draftkit.importPath`.

## Validation

- **Required Test Command**: `pnpm --filter @draftkit/core test`
- **Expected Result**: The integration suite passes and all previously green tests continue to pass.

## TDD Evidence

### RED

- Command: pnpm --filter @draftkit/core test
- Result: 8 AC-8 integration tests failed: incorrect relative path (4 levels up instead of 3) produced wrong story dir

### GREEN

- Command: pnpm --filter @draftkit/core test
- Result: 5 test files passed, 116 tests passed (9 new AC-8 integration tests green)

### REFACTOR

- Notes: The integration test reads real files from `examples/` — acceptable in test code per Art. 5. No changes to published package code are expected at this stage. Confirm `importPath` values in the test assertions match what is actually declared in each story file.

## Review Triage

- Priority: medium
- Owner: agent
- Notes: This task is a final integration gate. No new implementation is expected; if it fails it indicates a gap in T-001 or T-002.

## Done Checklist

- [ ] Implementation complete
- [ ] Tests pass
- [ ] Plan index updated
- [ ] Review triage filled
