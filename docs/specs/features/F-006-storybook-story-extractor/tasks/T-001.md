---
code_areas:
  - packages/draftkit-core/src/story-extractor.ts
  - packages/draftkit-core/src/story-extractor.test.ts
depends_on: []
doc_type: task-detail
feature_id: F-006
impact: code
last_updated: 2026-03-20
master_spec_id: MS-017
owner: agent
screens: []
status: Done
task_id: T-001
task_type: code
tdd_enforced: true
title: T-001 Add extractSpecFromStory — tests + implementation
---

# T-001: Add extractSpecFromStory — tests + implementation

## Objective

Write failing unit tests for all 6 ACs (AC-1 through AC-6) and then implement `extractSpecFromStory(content: string): ComponentSpec | null` in `packages/draftkit-core/src/story-extractor.ts`. All tests use in-memory story content strings as fixtures — no file I/O.

## Scope

- **Changed files**:
  - `packages/draftkit-core/src/story-extractor.ts` (new) — implementation of `extractSpecFromStory`
  - `packages/draftkit-core/src/story-extractor.test.ts` (new) — unit test suite for AC-1 through AC-6
- **Dependencies**: none

## Acceptance Criteria Covered

- AC-1: Returns `null` when `parameters.draftkit` block is absent from the default-exported meta object.
- AC-2: Returns a `ComponentSpec` whose `name` equals `parameters.draftkit.componentName`.
- AC-3: Sets `allowedProps` to the keys of `argTypes` excluding keys with an `action` property; `[]` when `argTypes` is absent.
- AC-4: Builds `propDefs` from `argTypes` using the control→type mapping (`select`→`enum`, `boolean`→`boolean`, `text`→`string`, `number`→`number`); sources `default` from `meta.args`; omits `default` when absent; `{}` when `argTypes` is absent.
- AC-5: Sets `description` and `importPath` from `parameters.draftkit`; both `undefined` when absent.
- AC-6: The returned `ComponentSpec` has no `component` key — the field is entirely absent, not set to `undefined`.

## Validation

- **Required Test Command**: `pnpm --filter @draftkit/core test`
- **Expected Result**: All unit tests for AC-1 through AC-6 pass; no existing tests regress.

## TDD Evidence

### RED

- Command: pnpm --filter @draftkit/core test
- Result: Import error: story-extractor.ts does not exist. 1 test file failed.

### GREEN

- Command: pnpm --filter @draftkit/core test
- Result: 5 test files passed, 100 tests passed (21 new AC-1–AC-6 tests green)

### REFACTOR

- Notes: Ensure edge cases from `spec.md` (e.g. unrecognised `control` values, `select` without `options`, `args` absent) are covered by distinct test cases. Extract AST-walking helpers to keep the implementation readable.

## Review Triage

- Priority: high
- Owner: agent
- Notes: `extractSpecFromStory` is the foundation for T-002 and T-003; must reach GREEN before those tasks start.

## Done Checklist

- [ ] Implementation complete
- [ ] Tests pass
- [ ] Plan index updated
- [ ] Review triage filled
