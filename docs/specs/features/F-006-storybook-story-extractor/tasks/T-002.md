---
code_areas:
  - packages/draftkit-core/src/story-extractor.ts
  - packages/draftkit-core/src/index.ts
depends_on:
  - T-001
doc_type: task-detail
feature_id: F-006
impact: code
last_updated: 2026-03-20
master_spec_id: MS-017
owner: agent
screens: []
status: Done
task_id: T-002
task_type: code
tdd_enforced: true
title: T-002 Add extractRegistryFromStories and export
---

# T-002: Add extractRegistryFromStories and export

## Objective

Write failing unit tests for AC-7 and AC-9, then extend `packages/draftkit-core/src/story-extractor.ts` with `extractRegistryFromStories(filePaths: string[]): Promise<ComponentRegistry>` and add both extractor functions as named exports from `packages/draftkit-core/src/index.ts`. Tests use in-memory fixture strings via mocked `node:fs/promises` — no real file I/O.

## Scope

- **Changed files**:
  - `packages/draftkit-core/src/story-extractor.ts` (extend) — add `extractRegistryFromStories`
  - `packages/draftkit-core/src/story-extractor.test.ts` (extend) — add unit tests for AC-7 and AC-9
  - `packages/draftkit-core/src/index.ts` (extend) — add named exports for both functions
- **Dependencies**: T-001 must be GREEN

## Acceptance Criteria Covered

- AC-7: `extractRegistryFromStories(filePaths)` reads each file, calls `extractSpecFromStory` on its content, returns a `ComponentRegistry` keyed by `name` containing only non-null results; files without a `parameters.draftkit` block are silently omitted.
- AC-9: Both `extractSpecFromStory` and `extractRegistryFromStories` are named exports of `@draftkit/core` (resolvable via `src/index.ts`).

## Validation

- **Required Test Command**: `pnpm --filter @draftkit/core test`
- **Expected Result**: All unit tests for AC-7 and AC-9 pass; no existing tests (AC-1–AC-6) regress.

## TDD Evidence

### RED

- Command: pnpm --filter @draftkit/core test
- Result: 2 AC-9 tests failed: extractRegistryFromStories and extractSpecFromStory not yet exported from index.ts

### GREEN

- Command: pnpm --filter @draftkit/core test
- Result: 5 test files passed, 107 tests passed (7 new AC-7/AC-9 tests green)

### REFACTOR

- Notes: Verify last-write-wins behaviour for duplicate `name` values. Confirm empty-array input returns `{}`. Confirm a non-existent path is skipped silently.

## Review Triage

- Priority: high
- Owner: agent
- Notes: Must reach GREEN before T-003 begins; the public export added here is required for T-003's integration test import.

## Done Checklist

- [ ] Implementation complete
- [ ] Tests pass
- [ ] Plan index updated
- [ ] Review triage filled
