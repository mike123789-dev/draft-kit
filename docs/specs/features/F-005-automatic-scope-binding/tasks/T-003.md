---
code_areas:
  - packages/draftkit-react/src/DraftKitShell.tsx
depends_on:
  - T-002
doc_type: task-detail
feature_id: F-005
impact: code
last_updated: 2026-03-20
master_spec_id: MS-020
owner: agent
screens: []
status: Done
task_id: T-003
task_type: implementation
tdd_enforced: true
title: T-003 Remove components prop from DraftKitShell
---

# T-003: Remove components prop from DraftKitShell

## Objective

Remove `components?: Record<string, unknown>` from `DraftKitShellProps` and from the `DraftKitShell` function signature. Update both `validateAndRenderJSX(jsxCode, registry, components)` call sites to `validateAndRenderJSX(jsxCode, registry)`. After this change, passing only a `registry` prop with embedded `component` references is sufficient for a working preview. The TypeScript props type must not include a `components` field.

## Scope

- **Changed files**: `packages/draftkit-react/src/DraftKitShell.tsx`
- **Dependencies**: T-002 (requires the updated `validateAndRenderJSX(code, registry)` signature)

## Validation

- **Required Test Command**: `pnpm --filter @draftkit/react test:unit && pnpm --filter @draftkit/react typecheck`
- **Expected Result**: All unit tests pass. `pnpm --filter @draftkit/react typecheck` reports zero errors. No caller passes a `components` prop to `DraftKitShell`.

## TDD Evidence

### RED

- Command: pnpm typecheck
- Result: FAILED: 2 TS2554 errors — Expected 2 arguments, got 3 (DraftKitShell.tsx lines 75, 79)

### GREEN

- Command: pnpm typecheck && pnpm test
- Result: PASSED: typecheck clean, 56/56 tests

### REFACTOR

- Notes: Run `pnpm --filter @draftkit/react typecheck` explicitly to confirm the `components` field is absent from the compiled type. Verify no F-003 tests were broken; if any tests passed `components` to `DraftKitShell`, update them to use `registry` with embedded `component` references instead.

## TDD Steps

1. **RED**: Add or update a `DraftKitShell` unit test that renders the shell with a `registry` prop whose entries include `component` references — and explicitly does NOT pass a `components` prop. Assert that the rendered output reflects the registry-provided components correctly. This test should fail (or the type should reject) before the `components` prop is removed.
2. **GREEN**: Remove `components?: Record<string, unknown>` from `DraftKitShellProps`. Remove the `components` parameter from the function signature. Update both `validateAndRenderJSX` call sites to `(jsxCode, registry)` (two arguments). Fix any TypeScript errors surfaced.
3. **REFACTOR**: Run `pnpm --filter @draftkit/react typecheck` to confirm zero type errors. Scan for any remaining test files that still pass `components` to `DraftKitShell` and update them. Run the full test suite one final time to confirm clean state.

## Review Triage

- Priority: high (removes a public prop — host-app callers must update)
- Owner: agent
- Notes: This is the intended breaking change to `DraftKitShell`'s public API per AC-6. `playground-next` (T-004) depends on this task completing successfully before it removes `playgroundComponents`.

## Done Checklist

- [ ] Implementation complete
- [ ] Tests pass
- [ ] Plan index updated
- [ ] Review triage filled
