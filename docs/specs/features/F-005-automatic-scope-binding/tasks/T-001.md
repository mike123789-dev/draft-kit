---
code_areas:
  - packages/draftkit-core/src/types.ts
depends_on: []
doc_type: task-detail
feature_id: F-005
impact: code
last_updated: 2026-03-20
master_spec_id: MS-020
owner: agent
screens: []
status: Done
task_id: T-001
task_type: implementation
tdd_enforced: true
title: "T-001 Add `component?: unknown` to ComponentSpec"
---

# T-001: Add `component?: unknown` to ComponentSpec

## Objective

Add an optional `component?: unknown` field to the `ComponentSpec` type in `@draftkit/core/src/types.ts`. This is a purely additive type change — no runtime logic changes. The field uses `unknown` (not `React.ComponentType` or any React-specific type) so `core` remains framework-agnostic.

## Scope

- **Changed files**: `packages/draftkit-core/src/types.ts`
- **Dependencies**: none

## Validation

- **Required Test Command**: `pnpm --filter @draftkit/core test:unit`
- **Expected Result**: All unit tests pass. TypeScript compiler accepts a `ComponentSpec` with a `component` value and one without — both satisfy the type without errors.

## TDD Evidence

### RED

- Command: pnpm --filter @draftkit/core typecheck
- Result: FAILED: 6 TS errors — 'component' does not exist in type 'ComponentSpec'

### GREEN

- Command: pnpm --filter @draftkit/core typecheck && cd packages/draftkit-core && pnpm test
- Result: PASSED: typecheck clean, 155/155 tests pass

### REFACTOR

- Notes: Confirm no other files in `@draftkit/core` duplicate the `ComponentSpec` shape. Verify `@draftkit/react` and `examples/playground-next` still compile (`pnpm --filter @draftkit/react typecheck`).

## TDD Steps

1. **RED**: Write a unit test (or type-level compile assertion) that constructs a `ComponentSpec` object with a `component` value (e.g., `component: () => null`) and asserts it is accepted by the type, and constructs one without `component` and asserts that is also valid. Before adding the field, the test with `component` set should fail (type error or unexpected runtime shape). A lightweight approach: write a runtime test that builds registry entries with and without `component` and asserts their shapes using `expect`.
2. **GREEN**: Add `component?: unknown` to the `ComponentSpec` interface/type in `packages/draftkit-core/src/types.ts`. All tests should now pass.
3. **REFACTOR**: Scan `@draftkit/core` for any duplicate type definitions that mirror `ComponentSpec`. Remove duplication if found. Run `pnpm --filter @draftkit/core test:unit` and `pnpm --filter @draftkit/core typecheck` to confirm clean state.

## Review Triage

- Priority: low (additive type change only)
- Owner: agent
- Notes: This unblocks T-002. No behavioral change — purely a type extension.

## Done Checklist

- [ ] Implementation complete
- [ ] Tests pass
- [ ] Plan index updated
- [ ] Review triage filled
