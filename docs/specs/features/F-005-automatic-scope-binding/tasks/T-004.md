---
code_areas:
  - examples/playground-next/src/app/page.tsx
depends_on:
  - T-003
doc_type: task-detail
feature_id: F-005
impact: code
last_updated: 2026-03-20
master_spec_id: MS-020
owner: agent
screens: []
status: Done
task_id: T-004
task_type: implementation
tdd_enforced: false
title: T-004 Consolidate playground registry
---

# T-004: Consolidate playground registry

## Objective

In `examples/playground-next/src/app/page.tsx`, remove the standalone `playgroundComponents` map. Build a single `playgroundRegistry` that spreads `defaultRegistry` and adds or overrides entries for all previously listed UI components (Badge, Button, Card, Input, Label, Separator, Tabs, TabsList, TabsTrigger, TabsContent, and the local Text stub) — each entry carrying both spec data and a `component` reference. Pass `registry={playgroundRegistry}` to `DraftKitShell` with no `components` prop.

## Scope

- **Changed files**: `examples/playground-next/src/app/page.tsx`
- **Dependencies**: T-003 (requires `DraftKitShell` to accept `registry` with embedded `component` refs and no `components` prop)

## Validation

- **Required Test Command**: `pnpm --filter playground-next lint && pnpm --filter playground-next typecheck`
- **Expected Result**: Both lint and typecheck pass with zero errors. No reference to `playgroundComponents` remains in `page.tsx`. No `components` prop is passed to `DraftKitShell`.

## Verification

This task is in the `examples/` app which has no unit test suite. Verification is lint + typecheck only.

After changes:

1. Run `pnpm --filter playground-next lint` — must exit with zero errors.
2. Run `pnpm --filter playground-next typecheck` — must exit with zero errors.
3. Manual browser verification is recommended: open the playground, confirm that the live preview renders Badge, Button, Card, and other registry-listed components correctly from JSX output. This manual step is not blocking for task sign-off but should be noted in the Done Checklist.

## TDD Steps

1. This task has no unit test suite. The verification gate is lint + typecheck.
2. Before making changes, confirm the current state: `pnpm --filter playground-next lint && pnpm --filter playground-next typecheck` should be passing (baseline).
3. Remove the `playgroundComponents` constant from `page.tsx`.
4. Build `playgroundRegistry` that spreads `defaultRegistry` and defines entries for every component previously in `playgroundComponents`, setting both spec fields and `component` on each entry.
5. Replace `registry={...} components={playgroundComponents}` (or equivalent) on the `DraftKitShell` JSX with `registry={playgroundRegistry}` (no `components` prop).
6. Run `pnpm --filter playground-next lint && pnpm --filter playground-next typecheck` — both must pass.

## Review Triage

- Priority: medium (examples/ only — no product package change)
- Owner: agent
- Notes: `tdd_enforced: false` because `examples/playground-next` has no unit test suite. Verification is lint + typecheck, with manual browser confirmation recommended. This is the final task in the F-005 sequence.

## Done Checklist

- [ ] Implementation complete
- [ ] Lint passes (`pnpm --filter playground-next lint`)
- [ ] Typecheck passes (`pnpm --filter playground-next typecheck`)
- [ ] Manual browser verification performed (recommended)
- [ ] Plan index updated
- [ ] Review triage filled
